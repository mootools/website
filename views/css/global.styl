/* F O N T S * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

@font-face {
	font-family: "Entypo";
	src: url("/fonts/entypo.eot");
	src: url("/fonts/entypo.eot?#iefix") format("embedded-opentype"),
		url("/fonts/entypo.woff") format("woff"),
		url("/fonts/entypo.ttf") format("truetype"),
		url("/fonts/entypo.svg#entypo") format("svg");
	font-weight: normal;
	font-style: normal;
}

@font-face {
	font-family: "Entypo Social";
	src: url("/fonts/entypo-social.eot");
	src: url("/fonts/entypo-social.eot?#iefix") format("embedded-opentype"),
		url("/fonts/entypo-social.woff") format("woff"),
		url("/fonts/entypo-social.ttf") format("truetype"),
		url("/fonts/entypo-social.svg#entypo-social") format("svg");
	font-weight: normal;
	font-style: normal;
}


/* R E S E T * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

/* Reset Browser Defaults */

blockquote, body, dd, div, dl, dt, fieldset, figure, form, h1, h2, h3, h4, h5, h6, legend, li, menu, ol, p, pre, ul {
	border: 0;
	margin: 0;
	padding: 0;
}

article, aside, details, figcaption, figure, footer, header, hgroup, main, nav, section, summary {
	border: 0;
	display: block;
	margin: 0;
	padding: 0;
}

ol, ul {
	list-style: none;
}


/* Forms */

button, input, select, textarea {
	-webkit-appearance: none;
	border: 0;
	-moz-box-sizing: content-box;
	-webkit-box-sizing: content-box;
	box-sizing: content-box;
	font-size: 100%;
	line-height: normal;
	margin: 0;
	padding: 0;
	text-transform: none;
	vertical-align: baseline;
}

button, html input[type="button"], input[type="reset"], input[type="submit"] {
	-webkit-appearance: button;
	cursor: pointer;
}

button::-moz-focus-inner, input::-moz-focus-inner {
	border: 0;
	padding: 0;
}

button[disabled], html input[disabled] {
	cursor: default;
}

input[type="checkbox"], input[type="radio"] {
	box-sizing: border-box;
	padding: 0;
}

input[type="search"] {
	-webkit-appearance: textfield;
	-moz-box-sizing: content-box;
	-webkit-box-sizing: content-box;
	box-sizing: content-box;
}

input[type="search"]::-webkit-search-cancel-button, input[type="search"]::-webkit-search-decoration {
	-webkit-appearance: none;
}

legend {
	white-space: normal;
}

textarea {
	overflow: auto;
	resize: vertical;
	vertical-align: top;
}


/* Typography */

html {
	font-size: 100%;
	-webkit-text-size-adjust: 100%;
	-ms-text-size-adjust: 100%;
	vertical-align: baseline;
}

sub, sup {
	font-size: 75%;
	line-height: 0;
	position: relative;
	vertical-align: baseline;
}

sub {
	bottom: -0.25em;
}

sup {
	top: -0.5em;
}

pre {
	white-space: pre;
	white-space: pre-wrap;
	word-wrap: break-word;
}

blockquote, q {
	quotes: none;
}

blockquote:before, blockquote:after, q:before, q:after {
	content: "";
	content: none;
}


/* Tables */

table {
	border-collapse: collapse;
	border-spacing: 0;
}


/* Images */

img {
	border: 0;
	max-width: 100%;
	height: auto;
	vertical-align: middle;
}

/* Horizontal Rule */

hr {
	border: 0;
	border-top: 1px solid #e5e3df;
	display: block;
	height: 0px;
	margin: 0;
	padding: 0;
}



/* G L O B A L * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

/* Body & Wrapper */

body {
	background: #ffffff;
	font-size: 1em;
}

/* Clearfix */

.clearfix:before, .clearfix:after {
	content: " ";
	display: table;
}

.clearfix:after {
	clear: both;
}

/* Hidden */

.hidden {
	display: none !important;
	visibility: hidden;
}

.visually-hidden {
	border: 0;
	clip: rect(0 0 0 0);
	height: 1px;
	margin: -1px;
	overflow: hidden;
	padding: 0;
	position: absolute;
	width: 1px;
}

/* Text Selection */

::-moz-selection {
	background: #777571;
	color: #ffffff;
	text-shadow: none;
}

::selection {
	background: #777571;
	color: #ffffff;
	text-shadow: none;
}

/* Links Style & Focus */

a {
	color: #777571;
	display: inline-block;
	text-decoration: none;
}

a:hover {
	color: #777571;
}

a:active, a:focus {
	background: #777571;
	-webkit-border-radius: 3px;
	-moz-border-radius: 3px;
	border-radius: 3px;
	color: #ffffff !important;
	outline: none;
}

/* Fonts */

body, button, html, input, select, textarea {
	color: #777571;
	font-family: "Source Sans Pro", "Helvetica", "Arial", sans-serif;
	font-weight: 300;
}

h1, h2, h3, h4, h5, h6 {
	font-weight: 200;
}

b, strong {
	font-weight: 400;
}

pre {
	background: #f4f1ed;
	-webkit-border-radius: 5px;
	-moz-border-radius: 5px;
	border-radius: 5px;
	font-family: "Source Code Pro", "Lucida Console", "Monaco", "Courier New", monospace;
	font-size: 0.8em;
	font-weight: 400;
	padding: 1em;
}

pre span.string {
	color: green;
}

/* Forms */
input, textarea, select {
	-webkit-appearance: none;
	background: #F4F1ED;
	-webkit-border-radius: 5px;
	-moz-border-radius: 5px;
	border-radius: 5px;
	color: #777571;
	cursor: pointer;
}

input:focus, textarea:focus, select:focus {
	outline: none;
}

/* Classes */

.button {
	background: #F4F1ED;
	-webkit-border-radius: 5px;
	-moz-border-radius: 5px;
	border-radius: 5px;
	color: #777571 !important;
	display: block;
	font-size: 1.1em;
	position: relative;
}

.button span {
	display: block;
	padding: 0.5em 0.6em;
	line-height: 1;
}

.button span span {
	padding: 0;
}

.button span span.icon {
	font-size: 400%;
	position: absolute;
	top: 0.3em;
	right: 0.1em;
	line-height: 0;
}

.button span.version {
	display: block;
	font-size: 0.8em;
	margin-top: 0.2em;
}

.button.highlighted {
	background: #acaaa7;
	color: #ffffff !important;
}

.button.highlighted .icon {
	color: #ffffff;
}

.lighter {
	color: #acaaa7;
}

a:active .lighter, a:focus .lighter {
	color: #ffffff;
}

.darker {
	color: #777571;
}

a:active .darker, a:focus .darker {
	color: #ffffff;
}

.summary {
	font-size: 1.4em;
}

.description {
	font-size: 1.2em;
}

.npm {
	background: #777571;
	color: #ffffff;
	margin: 1em 0;
}

.command {
	display: block;
	font-weight: 400;
	margin-bottom: 0.25em;
}

.wrapper {
	margin: 0 auto;
	max-width: 64em;
	padding: 0 4%;
}

/* Icons */

.arrow:after {
	content: "";
	font-family: "Entypo";
}

.icon {
	bottom: -0.1em;
	color: #acaaa7;
	font-size: 220%;
	line-height: 0;
	position: relative;
	vertical-align: baseline;
}

a:active .icon, a:focus .icon {
	color: #ffffff;
}

.download:after {
	content: "";
	font-family: "Entypo";
}

.github:after {
	content: "";
	font-family: "Entypo Social";
}

.twitter:after {
	content: "";
	font-family: "Entypo Social";
}

.facebook:after {
	content: "";
	font-family: "Entypo Social";
}

.googleplus:after {
	content: "";
	font-family: "Entypo Social";
}

.search {
	position: absolute;
	right: 0.25em;
	top: 0.25em;
	cursor: pointer;
}

.search:after {
	content: "\1F50D";
	font-family: "Entypo";
}


/* H E A D E R * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

#global-bar {
	padding-top: 1em;
	padding-bottom: 1em;
}

#logo {
	float: left;
}

#logo img {
	height: 24px;
}

#search {
	float: right;
}

#search label {
	display: block;
	position: relative;
}

#search input {
	font-size: 0.8em;
	padding: 0.3em 1em;
}

#header {
	border-bottom: #777571 1px solid;
}

#header a {
	color: #acaaa7;
}

#header a:active, #header a:focus {
	background: transparent;
}

#header a:active span, #header a:focus span {
	background: #777571;
	-webkit-border-radius: 5px;
	-moz-border-radius: 5px;
	border-radius: 5px;
}


/* M A I N * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

.docs a {
	position: relative;
}

.overview .summary {
	margin-bottom: 1.5em;
}

.overview .description {
	margin-bottom: 1.5em;
}

.overview .license {
	font-size: 0.8em;
	margin: 1em 0 3em;
}

.overview .use li {
	float: left;
	width: 50%;
}

.overview .use li a {
	margin: 0 0 0 0.35em;
}

.overview .use li:first-child a {
	margin: 0 0.35em 0 0;
}

.overview .use .button {
	height: 3em;
}

.features a {
	display: block;
	position: relative;
}


/* F O O T E R * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

#menu a {
	background: #f4f1ed;
	-webkit-border-radius: 5px;
	-moz-border-radius: 5px;
	border-radius: 5px;
	border: transparent 1px solid;
	display: block;
	padding: 0.6em 0 0.4em;
	text-align: center;
}

#menu a:active, #menu a:focus {
	border-color: #e5e3df;
}

#menu img {
	height: 1.6em;
}

#sitemap {
	font-size: 0.8em;
}

#sitemap h3 {
	color: #e5e3df;
	font-weight: 400;
}

#sitemap ul li a {
	display: block;
}

#sitemap ul li a span.icon {
	margin-right: 0.2em;
}

#credits {
	font-size: 0.8em;
}

#credits img {
	height: 2em;
	margin: 0 0 1em;
}


/* M O B I L E * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

@media only screen and (max-width: 47.999em) {  /* Less than 600 pixels */

	.arrow:after {
		font-size: 200%;
		margin-top: -0.15em;
		position: absolute;
		right: 0;
		top: 0;
	}

	.slider:after {
		content: "";
		float: right;
		font-family: "Entypo";
		font-size: 140%;
	}

	.overview {
		padding-top: 2em;
		padding-bottom: 180px;
		position: relative;
	}

	.overview .big-icon {
		width: 100%;
		bottom: 0;
		left: 0;
		height: 180px;
		overflow: hidden;
		position: absolute;
		z-index: -1;
		text-align: center;
	}

	.overview .big-icon img {
		height: auto;
		opacity: 0.3;
		max-width: 320px;
	}

	.overview .summary {
		font-size: 120%;
	}

	.overview .description {
		font-size: 100%;
	}

	.modules {
		padding-top: 1em;
		padding-bottom: 1em;
	}

	.modules div {
		margin-top: 1em;
	}

	.modules .docs {
		margin-top: 1em;
	}

	.modules .docs a {
		display: block;
	}

	.features {
		padding-top: 1em;
		padding-bottom: 1em;
	}

	.features section {
		margin-top: 1em;
	}

	.features section:first-child {
		margin-top: 0;
	}

	.features section p {
		max-width: 80%;
	}

	.features section .arrow:after {
		font-size: 400%;
		margin-top: 0.05em;
	}

	.features section a p {
		margin-top: 0.5em;
	}

	#header .wrapper {
		padding: 0;
	}

	#header h1 {
		padding: 0.5em 4%;
	}

	#header ul li a {
		display: block; padding: 0.5em 4%; border-top: 1px solid #e5e3df; cursor: pointer; display: none;
	}

	#header ul li.selected a {
		display: block;
	}

	#menu {
		margin-bottom: 1em;
	}

	#menu li {
		float: left;
		width: 50%;
		margin: 1em 0 0;
	}

	#menu li:nth-of-type(1n) a {
		margin: 0 0.5em 0 0;
	}

	#menu li:nth-of-type(2n) a {
		margin: 0 0 0 0.5em;
	}

	#footer.wrapper {
		padding: 0;
	}

	#sitemap div {
		border-bottom: 1px solid #e5e3df;
	}

	#sitemap div h3 {
		color: #acaaa7;
		cursor: pointer;
		padding: 0.5em 4%;
	}

	#sitemap div ul {
		display: none;
	}

	#credits {
		padding: 1em 4% 2em;
	}

}


/* T A B L E T * & * D E S K T O P * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

@media only screen and (min-width: 48em) {  /* More or equal to 768 pixels */

	.arrow:after {
		font-size: 160%;
		margin-left: 0.25em;
		position: relative;
		top: 0.10em;
		vertical-align: baseline;
	}

	.overview {
		padding-top: 2em;
		position: relative;
	}

	.overview .big-icon {
		bottom: 0;
		height: 230px;
		overflow: hidden;
		position: absolute;
		right: 4%;
		width: 400px;
		z-index: -1;
	}

	.overview .big-icon img {
		height: auto;
		opacity: 0.3;
		width: 100%;
	}

	.overview .use {
		max-width: 40%;
	}

	.modules {
		padding-top: 2em;
		padding-bottom: 2em;
	}

	.modules section {
		position: relative;
	}

	.modules div {
		margin-top: 2em;
		width: 48%;
	}

	.modules div:nth-of-type(1) {
		float: left;
	}

	.modules div:nth-of-type(2) {
		float: right;
	}

	.modules div pre {
		margin-top: 1em;
	}

	.modules .docs {
		position: absolute;
		right: 0;
		top: 0;
	}

	.features {
		padding-top: 2em;
		padding-bottom: 2em;
	}

	.features section {
		float: left;
		width: 33.333%;
	}

	.features section:nth-of-type(1) a {
		margin: 0 1.75em 0 0;
	}

	.features section:nth-of-type(2) a {
		margin: 0 1.25em 0;
	}

	.features section:nth-of-type(3) a {
		margin: 0 0 0 1.75em;
	}

	.features section a p {
		margin-top: 1em;
	}

	#header .wrapper div {
		position: relative;
	}

	#header h1 {
		padding: 1em 0;
		}

	#header nav {
		bottom: 0em;
		position: absolute;
		right: 0;
	}

	#header nav ul li {
		float: left;
		margin-left: 1em;
	}

	#header nav ul li:first-child {
		margin-left: 0;
	}

	#header nav ul li a {
		position: relative;
	}

	#header nav ul li a span {
		position: relative;
		top: -1em;
	}

	#header nav ul li.selected a {
		color: #777571;
	}

	#header nav ul li.selected a:after {
		background: #ffffff;
		border: 1px solid;
		border-color: #777571 #777571 transparent transparent;
		bottom: -6px;
		content: "";
		display: block;
		height: 10px;
		left: 50%;
		margin-left: -5px;
		position: absolute;
		-webkit-transform-origin: center;
		-moz-transform-origin: center;
		-ms-transform-origin: center;
		-o-transform-origin: center;
		transform-origin: center;
		-webkit-transform: rotate(-45deg);
		-moz-transform: rotate(-45deg);
		-ms-transform: rotate(-45deg);
		-o-transform: rotate(-45deg);
		transform: rotate(-45deg);
		width: 10px;
	}

	#menu {
		margin-top: 1em;
		margin-bottom: 1em;
	}

	#menu li {
		float: left;
		width: 25%;
	}

	#menu li:nth-of-type(1) a {
		margin: 0 0.6em 0 0;
	}

	#menu li:nth-of-type(2) a {
		margin: 0 0.45em 0 0.3em;
	}

	#menu li:nth-of-type(3) a {
		margin: 0 0.3em 0 0.45em;
	}

	#menu li:nth-of-type(4) a {
		margin: 0 0 0 0.6em;
	}

	#footer.wrapper {
		padding-top: 1em;
		padding-bottom: 3em;
	}

	#sitemap {
		float: right;
	}

	#sitemap div {
		float: left;
		margin-left: 2em;
	}

	#sitemap h3 {
		margin-bottom: 0.3em;
	}

	#credits {
		float: left;
	}

}



/* P A L E T T E * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

.prime .button.highlighted {
	background: #eb6f14;
}

.prime #header {
	border-color: #eb6f14;
}

.prime #header nav ul li.selected a {
	color: #eb6f14;
}

.prime #header nav ul li.selected a:after {
	border-color: #eb6f14 #eb6f14 transparent transparent;
}

.prime h2 {
	color: #eb6f14;
}

.prime #main a {
	color: #eb6f14;
}

.prime a:active, .prime a:focus, {
	color: #ffffff !important;
}

.prime a:active h2, .prime a:focus h2, {
	color: #ffffff !important;
}

